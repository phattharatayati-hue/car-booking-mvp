# บันทึก: ปัญหาต่อ MariaDB/MySQL ในเครื่อง (ไม่เกี่ยวกับโปรเจกต์จองรถ)

> เอกสารนี้แยกจาก `PROJECT.md` เพราะเป็นเรื่องของฐานข้อมูลในเครื่อง/วงแลน
> **โปรเจกต์จองรถใช้ Neon Postgres บนคลาวด์ ไม่เกี่ยวกับ MySQL/MariaDB ตัวนี้เลย**

---

## อาการที่เจอ

Navicat ต่อ `172.16.1.50:3306` แล้วขึ้น

```
1045 - Access denied for user 'root'@'172.18.0.1' (using password: YES)
```

## การวินิจฉัย

**1045 ไม่ใช่ปัญหา connection เต็ม**

| รหัส | ความหมาย |
|---|---|
| **1040** | Too many connections — คนอื่นเข้าไม่ได้เพราะเชื่อมต่อเต็ม |
| **1045** | Access denied — รหัสผ่านผิด หรือ user ไม่ได้รับอนุญาตจาก host นั้น |

ที่เจอคือ **1045** = เรื่องสิทธิ์ล้วนๆ ปิด container หรือปิดโปรแกรมที่รันอยู่ไม่ช่วย

**`172.18.0.1` คือ gateway ของ Docker bridge network**

แปลว่า connection วิ่งผ่าน Docker แล้วฝั่ง MySQL มองว่ามาจาก host นั้น

**ข้อสงสัยหลัก** — ใน Docker Desktop มี container `mariadb-1` map พอร์ต `3306:3306`
ถ้า `172.16.1.50` เป็น IP ของเครื่องตัวเอง Navicat ที่ต่อไป `172.16.1.50:3306`
จะ**ไม่ได้ไปถึงเซิร์ฟเวอร์ปลายทาง** แต่วิ่งเข้า container ตัวนี้แทน
→ กรอกรหัสของเซิร์ฟเวอร์จริง แต่ไปเทียบกับรหัส root ของ MariaDB ใน container → 1045

## วิธีตรวจ

```powershell
ipconfig                     # 172.16.1.50 เป็น IP ของเครื่องนี้หรือเปล่า
docker ps                    # ดูว่ามีอะไร map 3306 อยู่
```

ถ้าต่อเข้าเซิร์ฟเวอร์ได้แล้ว เช็คสิทธิ์กับจำนวน connection

```sql
SELECT user, host FROM mysql.user WHERE user = 'root';
SHOW STATUS LIKE 'Threads_connected';
SHOW PROCESSLIST;
```

## ทางแก้ตามสาเหตุ

| สาเหตุ | แก้ |
|---|---|
| `172.16.1.50` = เครื่องตัวเอง ชนกับ container | ใช้ IP ของเซิร์ฟเวอร์จริง หรือถ้าจะต่อ container ให้ใช้ `127.0.0.1:3306` + รหัสจาก `MARIADB_ROOT_PASSWORD` ใน compose file |
| grant ของ root มีแค่ `localhost` | สร้าง user สำหรับต่อจากข้างนอก อย่าเปิด root ให้ `%` |
| รหัสผ่านผิด | รีเซ็ตที่เครื่องเซิร์ฟเวอร์ |
| ถ้าเป็น 1040 จริง | เพิ่ม `max_connections` หรือหา connection ที่รั่วจาก `SHOW PROCESSLIST` |

---

## เรื่องพอร์ตชนกับโปรเจกต์จองรถ

**ไม่ชน** — ใช้พอร์ตและชนิดฐานข้อมูลคนละตัว

| ระบบ | พอร์ต | ฐานข้อมูล |
|---|---|---|
| car-booking (`npm run dev`) | 3000 | Neon **Postgres** บนคลาวด์ (ต่อออก 5432 ผ่าน TLS) |
| `mariadb-1` | 3306 | MariaDB ในเครื่อง |
| `phpmyadmin` | 81 | — |

เปิดหรือปิด container พวกนี้ไม่กระทบโปรเจกต์จองรถ และกลับกันด้วย

> หมายเหตุ: ใน Docker Desktop มี 21 containers ตรวจแล้วเฉพาะ 3 ตัวข้างบน
> ถ้าตัวอื่น map พอร์ต 3000 ไว้ จะชนกับ `npm run dev` — เช็คด้วย `docker ps` ก่อนรัน
